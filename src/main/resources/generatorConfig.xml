<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN" "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd" >
<generatorConfiguration>
    <!-- 引入配置文件 -->
    <properties resource="db.properties" />

    <context id="context1">

        <!-- 生成的Java文件的编码 -->
        <property name="javaFileEncoding" value="UTF-8" />

        <!-- 注释 -->
        <commentGenerator type="com.hanjianchun.MyCommentGenerator">
        </commentGenerator>

        <jdbcConnection driverClass="${oracle.jdbc.driverClass}"
                        connectionURL="${oracle.jdbc.url}" userId="${oracle.jdbc.username}" password="${oracle.jdbc.password}" >
        </jdbcConnection>

        <!-- 默认false，把JDBC DECIMAL 和 NUMERIC 类型解析为 Integer，为 true时把JDBC DECIMAL
            和 NUMERIC 类型解析为java.math.BigDecimal -->
        <javaTypeResolver>
            <property name="forceBigDecimals" value="false" />
        </javaTypeResolver>

        <!-- targetProject:生成PO类的位置 -->
        <javaModelGenerator targetPackage="com.zxl.model"
                            targetProject="${project}">
            <!-- 从数据库返回的值被清理前后的空格 -->
            <property name="trimStrings" value="true" />
        </javaModelGenerator>

        <!-- targetProject:mapper映射文件生成的位置 -->
        <sqlMapGenerator targetPackage="xml"
                         targetProject="${project}" />

        <!-- targetPackage：mapper接口生成的位置 -->
        <javaClientGenerator targetPackage="com.zxl.dao"
                             targetProject="${project}" type="XMLMAPPER" />

        <table tableName="ZXL_USER" domainObjectName="user"/>
    </context>
</generatorConfiguration>